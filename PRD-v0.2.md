# 《照见》产品需求文档

> 中元夜网络纪念作品｜开发版 v0.2

| 项目 | 内容 |
| --- | --- |
| 文档状态 | 待技术、视觉、隐私与运营评审 |
| 更新时间 | 2026-08-27 |
| 产品形态 | 移动端优先的响应式 Web |
| 首发日期 | 待确认，不在前端硬编码 |
| 核心任务 | 用户在 30–90 秒内，为想念的人完成一次不公开的点灯仪式 |

> **本版说明**：原 `PRD.md` 保留为概念完整版。本版删去重复叙述，冻结首版边界，并补充状态、数据、运营和验收规则。未确认的决策不得由前端自行猜测。

---

## 0. 决策摘要

### 0.1 定位

《照见》是一件中元夜参与式网络纪念作品。访客无需注册或填写身份信息，可以写一句话，也可以只点亮一盏无字灯。**私密灯仅保存在当前设备；只有用户主动同意公开、且通过审核的内容，才进入年度公共灯海。**

作品不模拟逝者、不提供通灵回应，不把纪念转化为社交排名、情绪依赖或付费消费。产品支持用户想念已经离开、暂时离开或仍在世的重要之人，不预设用户必须告别、放下或被治愈。

### 0.2 不可违背的约束

| 编号 | 约束 | 验收原则 |
| --- | --- | --- |
| C-01 | 私密优先 | 默认不公开；私密正文不上传服务器 |
| C-02 | 主动同意 | 公开必须由用户主动开启，并看到公开说明 |
| C-03 | 真实参与 | 环境光可程序生成，代表用户的灯和文字不得伪造、复制或虚构数量 |
| C-04 | 不替用户表达 | 不自动生成、润色、补写或模拟任何人的回应 |
| C-05 | 核心流程可用 | 网络、WebGL、声音或高性能图形不可用时，仍可完成私密点灯 |
| C-06 | 可撤回 | 公开功能启用时，必须同时提供可执行的撤回、删除和紧急下架机制 |

### 0.3 首版成功定义

首版只验证私密仪式是否成立：

- 目标用户无需注册即可开始并完成点灯。
- 允许称呼和留言同时为空。
- 核心点灯完成时长目标为 30–90 秒，不含用户自主停留和制卡时间。
- 默认纪念卡不暴露称呼、留言或参与数量。
- 记录匿名体验指标，但不建立悲伤、纪念兴趣或身份画像。

---

## 1. 发布范围与门槛

### 1.1 P0：私密仪式模式（无公开数据也可上线）

**必须包含**：

1. 首页、作品说明、隐私说明和内容规则入口。
2. 可选称呼、可选留言；支持无字灯。
3. 默认私密的本地保存。
4. 触摸、鼠标、键盘均可完成三秒持续操作点灯。
5. 个人灯和程序环境的低成本视觉表现。
6. 静态纪念卡生成、预览、保存和失败回退。
7. WebGL/Canvas 不可用、弱网、离线、减少动态时的核心流程。
8. 本地数据查看、导出（如实现成本可接受）和清除入口，并告知清除后不可恢复。
9. 匿名错误、性能和核心流程埋点。

**P0 不包含**：公开提交、公开浏览、服务端灯海、审核、举报、删除令牌、实时更新、年度公共档案。

### 1.2 条件功能：公开模式

公开模式不是无条件 P0。仅当以下前置条件全部完成，才允许打开服务端 feature flag：

- 已确认数据处理主体、部署地区、隐私政策和公开内容保存期限。
- 有明确的人工审核负责人、开放期值守安排和审核 SLA。
- 已上线审核队列、举报、撤回、删除、审计日志和紧急关闭。
- 已完成高风险内容升级与地区化危机资源方案。
- 已通过限流、XSS/恶意链接、幂等重试、缓存删除和恢复演练。

任一条件不满足：前端不展示公开开关、公开浏览入口或公开提交接口；私密模式照常运行。前端隐藏不是安全控制，服务端必须拒绝公开写入。

### 1.3 P1/P2

| 版本 | 内容 |
| --- | --- |
| P1 | 条件公开模式稳定化、公共灯浏览、年度只读档案、最低限度运营后台、分享落地页 |
| P2 | 多年度档案、实时/准实时灯海、动态卡、无障碍文本浏览、线下装置、多语言和个人纪念册 |

---

## 2. 产品状态与用户流程

### 2.1 年度状态

服务端配置返回唯一的 `artwork_year`、公历日期、农历日期、时区、开放窗口和状态。前端不得硬编码年份、日期或“农历七月十五”。

| 状态 | 用户可做什么 | 公开能力 |
| --- | --- | --- |
| `preview` 预告 | 查看作品、开放时间和上一年度档案（若有） | 关闭 |
| `open` 开放 | 完成私密点灯；公开模式启用时可主动提交公开 | 由 feature flag 决定 |
| `archive` 档案 | 查看年度公共档案；原设备查看仍保留的私密灯 | 不接受新点灯和新公开提交 |

默认时间规则：开放窗口使用项目配置时区；跨午夜时，整个窗口归属配置的年度。客户端时钟只用于展示辅助，状态和截止判断以服务端时间为准；服务端不可用时进入私密降级模式。

### 2.2 点灯状态机

```text
idle
  → drafting
  → holding
  → private_completed
  → releasing (8–10 秒本地升空过渡，可跳过)
  → card_preview / done

private_completed + 用户主动公开
  → public_pending
  → public_submitted
  → approved | rejected | removed

holding --提前松手/失焦--> drafting
任何状态 --用户取消公开--> private_completed
public_pending --网络断开--> pending_local_retry
pending_local_retry --用户主动重试--> public_submitted
```

规则：

- `private_completed` 是核心成功状态，不依赖服务器。
- 未完成三秒持续操作前，不发送公开正文。
- 公开待发送内容只在用户主动选择公开后产生；仅在用户主动重试时提交，不静默自动提交。
- `rejected`、`removed` 不删除当前设备的私密灯，除非用户主动清除。
- 同一逻辑灯使用 `idempotency_key` 防止重复提交；重复请求返回原结果。
- 开放期结束后，禁止新的公开提交；已提交内容仍按审核策略处理，并在界面告知状态。

### 2.3 主流程

```text
进入首页
  → 立即看到主题与「留一盏灯」
  → 输入称呼/留言（均可跳过）
  → 查看公开说明（默认关闭）
  → 持续按住 3 秒
  → 当前设备生成一盏灯
  → 用户停留、生成默认纪念卡或清除数据
```

“先在这里看看”作为辅助入口，不阻塞主按钮；入场动画只增强氛围，不要求用户等待或先读完文案。

---

## 3. 功能需求

### FR-01 首页与进入

**内容**：首屏立即出现“为想念的人留一盏灯”和主按钮 `留一盏灯`；辅助入口为 `先在这里看看`。可使用以下文案，但日期变量必须运行时注入：

> 有些人离开很久了。  
> 却仍在我们生活的细节里。

> 今晚，为想念的人留一盏灯。

**规则**：

- 主题和可操作入口在首屏核心内容 P75 ≤ 2.5 秒可见。
- 入场动画最长 6 秒，但不得阻塞按钮；支持跳过，回访可缩短。
- 默认静音，不展示数量、在线人数或公开留言。
- 底部提供作品说明、隐私说明和举报/联系入口（公开模式启用时）。

### FR-02 称呼与留言

- 称呼可空，最多 12 个汉字或 24 个 Unicode 字符。
- 留言可空，最多 60 个汉字或 120 个 Unicode 字符，最多三行。
- 前端提示不要填写手机号、地址、身份证号等信息；服务端公开审核仍必须防护。
- 不自动生成、补写、润色或纠正内容。
- 默认公开开关关闭。启用公开前展示“审核后公开、请勿填写可识别信息、可撤回”的说明。
- 输入内容在 `draft` 状态写入本地；用户清除或过期后按本地数据规则处理。

### FR-03 持续操作点灯

- 触摸、鼠标和键盘均支持持续 3 秒操作。
- 按下开始计时；提前松开、失焦、切换标签页或触摸滚动均取消本次计时，不判定为失败。
- 提前结束提示：`它还在等你。`，用户可自然重试。
- 键盘支持 Space；禁止 Space 导致页面滚动，Enter 提供等价的可访问替代操作。
- 屏幕阅读器可读取“正在点亮”“还需约 2 秒”“已经亮了”等状态；不依赖声音或颜色。
- 完成前不得向服务器提交公开内容。

### FR-04 本地灯

每次完成生成 `local_lamp_id` 和非身份 `lamp_seed`。种子可使用点灯时间分桶、留言长度、操作节奏和客户端随机数，不得使用姓名、留言正文、设备指纹或精确位置决定外观。

私密灯包含抽象光核、光晕、呼吸周期和艺术化坐标。坐标只用于视觉标识，不代表排名、地理位置或参与人数。

### FR-04A 升灯过渡

- 三秒持续操作完成后，先写入当前设备的私密灯，再进入 8–10 秒的本地升空过渡；过渡不影响私密存储语义，也不等待网络。
- 灯从场景下方缓慢升至夜空，使用抽象暖金纸罩、微弱光芯和轻微漂移；不使用写实火焰、强粒子、声音或其他用户灯的模拟效果。
- 默认完整播放，用户可主动跳过并立即进入完成状态；动画结束事件失效时，必须有一次性兜底完成路径。
- 减少动态效果开启时，直接进入完成状态，仍提供“已经亮了”的等价文本反馈。
- 升空期间不显示纪念卡入口；动画结束或跳过后才展示。动画不携带、展示或上传称呼和留言，不代表公共参与数量。

### FR-05 灯海与低流量

P0 只展示当前设备的私密灯和程序环境；公开模式启用后，服务端返回的审核通过灯另行标识。环境粒子不得伪装成真实纪念灯，也不得复制真实灯制造人数感。

| 可展示的真实公开灯档位 | 视觉策略 |
| --- | --- |
| 0 | 个人灯近景、留白、微弱倒影和环境层 |
| 1–5 | 疏落近景/中景 |
| 6–30 | 松散灯群，可游走 |
| 31+ | 拉远镜头；接口只返回展示所需数据，不返回总数或可推导总量 |

阈值是内部配置，不作为对外文案或埋点字段。私密灯、审核中、拒绝和删除灯不计入公共展示档位。

### FR-06 纪念卡

点灯完成后展示 `带走这束光`。默认卡只包含项目名称、动态生成的实际点灯日期和固定祝语，不含称呼、留言、灯序号或数量。

若用户希望添加内容：

1. 称呼和留言分开勾选，默认均关闭。
2. 生成前展示卡片预览和隐私提醒。
3. 用户二次确认后生成静态长图。
4. 失败时提供纯文本和简化图片回退。

卡片尺寸、格式、字体、最大体积、二维码短链有效期和微信保存失败替代路径，须在视觉/技术评审中冻结；二维码不得携带或暴露私密正文。

### FR-07 公开灯（条件功能）

用户主动开启公开并完成点灯后，创建 `submission_id` 和 `idempotency_key`。服务端生成 `public_lamp_id`，二者与 `local_lamp_id` 的映射不向其他用户暴露。

- 状态依次为 `pending`、`approved`、`rejected`、`removed`。
- 仅 `approved` 内容可进入公共灯海。
- 审核中展示“这句话会在确认适合公开后，进入更远的夜色”。
- 未通过展示“这句话将只为你保留”，不使用惩罚性文案。
- 公开浏览每次只展示一条审核通过留言，不支持搜索、点赞、评论、单条转发或热度排序。
- `public_browse_min_approved` 阈值由服务端配置；不足时不展示入口、不使用样例留言。

### FR-08 本地数据与清除

建议使用 IndexedDB（必要时降级为受容量限制的 localStorage），至少区分：

```text
schema_version
- draft
- private_completed
- public_pending_local
- public_submission_reference
```

必须明确并实现：写入时机、草稿和待发送内容保存期限、刷新恢复优先级、多标签页冲突、隐私模式失败提示、用户清除入口。清除站点数据后不承诺恢复；待发送公开内容撤销后立即清理正文。

### FR-09 作品说明

作品说明必须明确：

- 私密灯只在当前设备保存，公开灯须主动同意且审核通过。
- 环境光不等于其他用户的灯。
- 不提供通灵、祈福结果或逝者回应。
- 公开内容的保存期限、审核、举报、撤回和删除路径。
- 项目主体、联系方式、部署地区、隐私政策和未成年人处理规则。

---

## 4. 数据、隐私与安全

### 4.1 数据分层

| 数据 | P0 存储位置 | 公开模式 |
| --- | --- | --- |
| 称呼、留言正文 | 当前设备本地 | 仅主动公开后提交审核所需字段 |
| 灯视觉种子 | 当前设备本地 | 可生成非身份公开视觉种子 |
| 点灯时间 | 当前设备本地，内部可保存粗粒度时间 | 公开数据仅按必要性保存 |
| 身份、精确位置、设备指纹 | 不采集 | 不采集 |
| 公开同意版本 | 不适用 | 服务端保存同意版本和时间 |

“不采集正文埋点”不等于第三方 SDK 自动不采集；上线前必须审查所有分析、错误监控、CDN 和日志配置，确保正文不会进入 URL、日志、崩溃报告或缓存键。

### 4.2 公开对象最小字段

```text
public_lamp_id
artwork_year
display_label (optional)
message (optional)
moderation_status
public_consent_version
created_at (仅用于必要排序)
deleted_at (必要时)
deletion_token_hash
```

删除令牌只存摘要；令牌一次展示、不可猜测、有限期、限速，丢失时提供人工申请删除渠道。删除必须同步处理数据库、缓存、搜索索引和公共展示。

### 4.3 内容安全

公开内容至少拦截个人联系方式、地址、身份证号、二维码、广告引流、侮辱诽谤、仇恨、色情、暴力、违法内容、针对个人的伤害性指控以及明确的自伤/伤人风险。高风险内容不得自动公开，必须有人工升级路径和发布地区对应的危机资源。

未成年人策略、数据主体请求、跨境/备案、保存期限和法律责任人是上线阻塞项，不以本 PRD 的原则描述替代正式确认。

---

## 5. 视觉、性能与无障碍

### 5.1 视觉原则

墨黑、月白、暖金；抽象光体；留白、慢速呼吸和空间层次。避免惊悚、宗教仪式模拟、霓虹故障、写实蜡烛和高刺激动效。环境层与真实灯必须在形态、说明和数据来源上可区分。

### 5.2 渲染与性能预算

首版优先采用 CSS 光晕 + Canvas 2D 的单一可维护路径；WebGL 作为后续增强，不因追求多渲染路径推迟核心验证。

- 中端移动设备、配置化 4G 弱网下，首屏核心文字和按钮 P75 ≤ 2.5 秒。
- 点灯交互在目标设备上 P95 ≥ 30 FPS；低性能设备减少粒子、模糊和反射，但保留进度反馈。
- 纪念卡生成 P95 ≤ 3 秒；失败提供纯文本/简化图。
- 兼容 iOS Safari、Android Chrome、微信内置浏览器及桌面 Chrome/Safari/Edge 的当前支持版本；测试横竖屏切换。

### 5.3 无障碍

- 所有输入有语义标签，主按钮可通过键盘聚焦。
- 持续操作有可见进度和非视觉状态反馈。
- 支持减少动态效果；核心功能不依赖声音、颜色或触觉。
- VoiceOver、TalkBack、键盘和鼠标各至少完成一次端到端验收。

---

## 6. 审核与运营

公开模式必须配套最小后台：年度/状态配置、公开开关、审核队列、举报处理、删除/撤回、紧急关闭、操作审计和匿名指标查看。

上线前须冻结：

- 审核角色、班次、SLA 和高峰期超时策略。
- 自动预检是否使用、误杀处理和申诉路径。
- 举报对象、原因、重复举报和恶意举报处理。
- 高风险内容升级责任人、地区化危机资源和应急响应。
- 配置发布审批、回滚和缓存刷新。
- 开放结束时正在长按、待发送和审核中的内容如何处理。

任何时刻无法保证审核值守：服务端关闭公开写入和公共浏览，保留 P0 私密点灯。

---

## 7. 埋点与评估

埋点只用于判断体验是否顺畅，不用于建立悲伤或纪念兴趣标签。不得上传称呼、留言、卡片内容、精确位置、设备指纹或可识别 URL。

建议事件：

```text
artwork_view
light_start
compose_complete
hold_start
hold_early_release
light_complete
card_generate / card_save
local_draft_restore
public_opt_in
public_submit_result
public_retry
render_fallback
reduced_motion_enabled
report_submit
withdraw_complete
```

事件字段只保留年度、产品状态、能力分桶和错误类别。上线前为每个指标定义目标值、采样率、保留期、访问权限和隐私审查结果。首版重点观察点灯完成率、提前松手率、卡片生成成功率、核心错误率和降级后完成率，不以公开量或灯海规模作为成功标准。

---

## 8. 验收标准

### 8.1 P0 必须通过

1. Given 用户首次进入且网络不可用，When 他们点击主按钮，Then 仍可完成私密输入、三秒持续操作和本地灯生成。
2. Given 称呼和留言均为空，When 他们完成持续操作，Then 生成无字灯，不出现强制解释提示。
3. Given 用户未主动开启公开，When 他们完成点灯，Then 正文不离开当前设备，服务端无公开提交。
4. Given 用户提前松手、失焦或滚动，When 本次持续操作中断，Then 计时取消、状态自然回落，可再次操作且无惩罚性错误。
5. Given 用户使用触摸、鼠标、键盘或屏幕阅读器，When 他们完成核心流程，Then 均能获得等价结果和状态反馈。
6. Given WebGL 不可用或用户启用减少动态，When 他们点灯，Then 降级效果仍保留进度、完成反馈和卡片生成。
7. Given 0、1、5、30 或 31+ 条真实公开灯测试夹具，When 渲染灯海，Then 不复制真实灯、不返回或展示参与总数，构图按档位变化。
8. Given 用户生成默认纪念卡，When 他们预览并保存，Then 卡片不含私人正文、灯序号、数量或虚构参与文案。
9. Given 卡片生成失败，When 用户选择回退，Then 可获得可读纯文本或简化图片。
10. Given 用户清除站点数据，When 他们再次访问，Then 系统不声称能够恢复私密灯，并提供清除说明。
11. Given 服务端不可用，When 页面初始化，Then 隐藏公开入口并保留私密核心流程；服务端拒绝公开写入。
12. Given 年度状态为 `preview` 或 `archive`，When 用户访问，Then 页面不接受新点灯，并展示对应状态文案。

### 8.2 公开模式额外通过项

1. 未经主动同意或审核通过的正文不进入公共展示。
2. `pending`、`rejected`、`removed` 的本地和公共视觉状态符合状态机。
3. 重试同一 `idempotency_key` 不产生重复公开灯。
4. 用户可通过令牌或人工渠道撤回；删除传播到数据库、缓存和公共展示。
5. 审核、举报、删除、配置和紧急关闭均有可审计记录。
6. 审核资源不可用时，公开写入和公共浏览可在服务端熔断。

---

## 9. 上线阻塞决策

以下事项必须在开发冻结前由责任人确认：

1. 首发年份、开放窗口、年度归属、时区和服务端时间源。
2. 部署地区、域名、数据处理主体、备案/跨境要求和隐私政策。
3. 本地存储方案、保存期限、导出/清除规则和待发送公开内容期限。
4. 是否启用公开模式；若启用，审核负责人、值守、SLA、危机资源和熔断权限。
5. 公共内容保存期限、删除渠道、删除令牌策略和缓存删除时限。
6. 目标设备清单、性能预算、卡片输出规格和微信保存方案。
7. 是否支持在世/暂时离开的重要之人，并据此冻结首屏和审核文案。
8. 项目署名、联系方式和对外传播范围。

**默认上线方案**：私密模式、静态纪念卡、CSS/Canvas 降级、服务端不接收私密正文、公开能力关闭、前台不展示任何参与数量。

---

## 10. 后续产出

1. 低保真流程图：覆盖年度状态、点灯状态机和异常分支。
2. 数据与隐私方案：本地 schema、公开接口、删除和日志策略。
3. 首页、输入、持续操作、灯和纪念卡视觉原型。
4. 0/1/5/30/31+ 灯的渲染测试夹具。
5. 真机、弱网、无障碍和降级测试矩阵。
6. 若公开模式获准，补充审核后台、运营 SOP 和应急演练记录。

---

## 附录 A：核心文案

| ID | 场景 | 文案 |
| --- | --- | --- |
| `COPY_HOME_THEME` | 首页主题 | 为想念的人留一盏灯 |
| `COPY_HOME_INVITE` | 首页邀请 | 今晚，为想念的人留一盏灯。 |
| `COPY_INPUT_PERSON` | 称呼 | 此刻，你想起了谁？ |
| `COPY_INPUT_MESSAGE` | 留言 | 如果只能留一句话，你想对 Ta 说什么？ |
| `COPY_HOLD` | 持续操作 | 请轻轻按住它。让这句话在今晚亮起来。 |
| `COPY_HOLD_CANCEL` | 中断 | 它还在等你。 |
| `COPY_COMPLETE` | 完成 | 已经亮了。 |
| `COPY_PRIVATE` | 私密说明 | 这句话将只为你保留。 |
| `COPY_PUBLIC_PENDING` | 审核等待 | 这句话会在确认适合公开后，进入更远的夜色。 |
| `COPY_OFFLINE` | 离线 | 今晚的路有些远。这盏灯暂时留在你身边。 |
| `COPY_ARCHIVE` | 档案 | 那一夜，灯曾在这里亮起。你仍然可以回来看看。 |

## 附录 B：原版内容处理

- 产品理念、参考案例和完整风险背景：保留在原 `PRD.md`，供概念和创作评审使用。
- 重复出现的伦理边界：以本版 C-01～C-06 为唯一开发引用。
- 视觉稿、文案稿和法律/隐私正式文本：在本版确认后分别建立独立文档，不继续堆入 PRD。
