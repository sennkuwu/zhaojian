# 《照见》开发规范

## 目标
依据 `PRD-v0.2.md` 实现移动端优先的 P0 私密纪念仪式。原 `PRD.md` 与 `PRD-v0.2.md` 是需求真源。

## P0 边界
实现首页、可选称呼/留言、默认私密、三秒持续点灯、本地灯、CSS/Canvas 降级、静态纪念卡、本地数据恢复/清除、说明和无障碍。公开提交、审核、公共灯海、账号和服务端正文存储不实现、不展示。

## 隐私红线
- 私密正文只进入当前设备的 IndexedDB/localStorage，不发送网络请求。
- 不接入第三方分析、错误监控、远程字体或远程图片。
- 不采集身份、精确位置、设备指纹；正文不进入 URL、日志或埋点。
- 默认纪念卡不包含称呼和留言，清除数据必须明确不可恢复。

## 目录约定
- `src/components/` React UI 组件
- `src/lib/` 纯业务逻辑、存储和卡片生成
- `src/types/` 类型定义
- `src/styles/` 全局样式
- `public/` 同源静态资源和运行时配置

## 验证命令
- `npm run check`：TypeScript 检查
- `npm run build`：生产构建
- `npm run test`：Vitest 单元测试

## 实现原则
优先可用、克制和降级路径。不要硬编码年份或日期；不以动画阻塞主流程；状态迁移使用明确类型；改动后运行验证命令。
